import { OCRResult } from "../types/OCRResult";

export class OCRRunner {
  static async runOCR(imageBuffer: Buffer): Promise<OCRResult[]> {
    // 🔹 placeholder: na razie zwraca pustą tablicę
    return [];
  }
}