// src/modules/quickadd/commands/QuickAddCommandRegistry.ts
import { Client } from "discord.js";

/**
 * Placeholder do rejestracji komend QuickAdd.
 * Na razie loguje tylko, że komendy zostały zarejestrowane.
 */
export function registerQuickAddCommands(client: Client) {
  console.log("[QuickAdd] Placeholder: QuickAdd commands registered.");
}