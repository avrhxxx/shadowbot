// src/quickadd/integrations/EventsIntegration.ts

import { ParsedEntry } from "../core/QuickAddTypes";

export async function addToEvent(
  guildId: string,
  entries: ParsedEntry[]
) {
  // 🔥 placeholder – tu podłączysz eventService
}